# Conversation Log — 2026-04-28 20:37

# 참여자
- Toby (사용자)
- Claude (Opus 4.7)

## 브랜치
- `main`

---

## 1. Processes 패널 제거 + 데이터 흐름 정리
사용: Bash, Read, Edit

Toby: process 항목은 제거해달라.

Claude:
- 위치 확인: `src/ui.ts:540–556` (Processes 박스 렌더), `src/types.ts:137–141` (`ChildProcess` 인터페이스), `src/types.ts:217` (`childProcesses` 필드), `src/parser.ts:119` (초기화), `src/parser.ts:1014–1055` (`loadProcessInfo`).
- UI에서 "Processes" 박스 블록 삭제 (`if (state.childProcesses.length > 0) { ... }` 통째로 제거).
- 사용처 사라진 `ChildProcess` 인터페이스와 `SessionState.childProcesses` 필드 삭제. CLAUDE.md 가이드("미사용은 완전 삭제, shim 금지")에 따라 reexport / `_ =` 같은 호환 잔재 없이 정리.
- `loadProcessInfo`도 단순화: `ps -eo pid,ppid,rss,comm`으로 머신 전체 프로세스 테이블을 훑던 코드를 `ps -o rss= -p <pid>`로 좁혀, 모니터링 중인 세션 PID의 RSS만 읽도록 변경. 헤더의 `Mem:XXMB` 칩이 유일한 소비자라서 자식 스캔이 더 이상 필요 없음.
- `bun run build` 통과 — 7 modules, 54ms compile. TypeScript LSP에서 보이는 `process` / `fs` 진단은 `@types/node` 미설치 환경 이슈라 기존부터 있던 것이고 이번 변경과 무관.

---

## 2. commit & push
사용: Bash, Skill (toby-essentials:save-conversation)

Toby: commit and push.

Claude:
- `git status` / `git diff --stat` 확인 — 변경 3 파일 (`src/parser.ts`, `src/types.ts`, `src/ui.ts`), `+8 / -59` lines.
- 최근 커밋 메시지 스타일 확인 (`git log -3 --format=full`): imperative subject, body로 why, `Co-Authored-By` trailer.
- 첫 커밋 시도 시 `save-conv-before-commit.sh` 훅이 conversation log stale로 차단 → save-conversation skill 실행 후 재시도 예정.

---

## 변경된 파일
- `src/parser.ts` (수정)
- `src/types.ts` (수정)
- `src/ui.ts` (수정)
- `conv-logs/202604/28/conv-20260428-203709.md` (추가)
