# Conversation Log — 2026-04-24

# 참여자
- Toby (사용자)
- Claude (Opus 4.6)

## 브랜치
- `main`

---

## 1. Quota 패널이 안 보이는 문제 — OMC 캐시 fallback 추가
사용: Read, Edit, Bash, Explore agent

Toby: Quota가 안 보여 — rate limit quota 행이 대시보드에 렌더링되지 않음.

Claude: 원인 분석 후 OMC의 `.usage-cache.json`을 fallback 데이터 소스로 추가:
- `loadRateLimitFromOmc()` 함수 추가 (OMC OAuth API 캐시 읽기)
- `RateLimitInfo` 타입 업데이트: `sevenDayPct` → `weeklyPct`, `sonnetWeeklyPct`/`opusWeeklyPct` 추가
- `ui.ts` 필드명 및 라벨 업데이트 (`wk:` 사용)
- 리셋 시간이 과거인 경우 음수 duration 표시 버그 수정 (`resetCountdown` 헬퍼)

---

## 2. OMC 의존성 제거 — Anthropic OAuth API 직접 호출로 전환
사용: Read, Edit, Bash, Explore agent

Toby: oh-my-claudecode의 정보를 사용하는 건 잘못됐어. quota를 가져오는 다른 방법을 찾아봐.

Claude: OMC 캐시 의존성을 제거하고 Anthropic OAuth API를 직접 호출하도록 재구현:
- macOS Keychain에서 OAuth 토큰 읽기 (`/usr/bin/security find-generic-password`)
- `curl`로 `api.anthropic.com/api/oauth/usage` 직접 호출 (동기식, `execFileSync`)
- 로컬 캐시 (`~/.claude/ccmonitor-usage-cache.json`) 5분 TTL
- API 응답의 `utilization` 값이 이미 퍼센트임을 확인하여 `* 100` 버그 수정
- abtop 파일을 fallback으로 유지 (non-macOS 환경용)
- 테스트 결과: 5h: 14%, wk: 3% — API 응답과 일치

---

## 변경된 파일
- `src/parser.ts` (수정)
- `src/types.ts` (수정)
- `src/ui.ts` (수정)
