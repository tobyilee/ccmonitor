# Conversation Log — 2026-04-24

# 참여자
- Toby (사용자)
- Claude (Opus 4.6)

## 브랜치
- `main`

---

## 1. Fix Quota panel not showing — add OMC usage cache as data source
사용: Read, Edit, Bash, Explore agent

Toby: Quota가 안 보여 — the rate limit quota row wasn't rendering in the dashboard.

Claude: Root cause: `loadRateLimit()` only read `~/.claude/abtop-rate-limits.json`, which doesn't exist on this machine. Investigated OMC's rate limit architecture (OAuth API via `usage-api.ts`, cached to `~/.claude/plugins/oh-my-claudecode/.usage-cache.json`).
- Added `loadRateLimitFromOmc()` as fallback data source reading OMC's `.usage-cache.json`
- Updated `RateLimitInfo` type: renamed `sevenDayPct` → `weeklyPct`, added `sonnetWeeklyPct`/`opusWeeklyPct` fields
- Updated `ui.ts` to use renamed fields (`weeklyPct`/`weeklyResetsAt`, label `wk:` instead of `7d:`)
- Converted OMC's ISO date strings to epoch seconds for consistent reset time handling
- Build verified, installed globally

---

## 변경된 파일
- `src/parser.ts` (수정)
- `src/types.ts` (수정)
- `src/ui.ts` (수정)
