# Conversation Log — 2026-04-24

# 참여자
- Toby (사용자)
- Claude (Opus 4.6)

## 브랜치
- `main`

---

## 1. Session switcher (`n` key) 동작하지 않는 문제 수정
사용: Read, Edit, Bash

Toby: n 키로 next session으로 전환하는 기능이 동작하지 않아.

Claude: 원인 분석: `~/.claude/sessions/<pid>.json` 레지스트리 파일의 sessionId가 stale (Claude Code가 프로세스 시작 시 한 번만 기록하고, 같은 프로세스에서 새 대화를 시작해도 업데이트하지 않음).
- 레지스트리의 sessionId `c7797927` (April 19)와 실제 최신 transcript `4c110057` (April 24) 불일치 확인
- `loadActiveSessions()`에 `resolveLatestSessionId()` 함수 추가 — 레지스트리 sessionId 대신 해당 cwd의 transcript 디렉토리에서 가장 최근 수정된 `.jsonl` 파일명으로 실제 sessionId를 resolve
- 테스트 결과: `Sess:2 (+tobycodex)` 정상 표시, 올바른 sessionId로 resolve됨

---

## 변경된 파일
- `src/parser.ts` (수정)
